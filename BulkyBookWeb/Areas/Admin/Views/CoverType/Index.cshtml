@model IEnumerable<BulkyBook.Models.CoverType>

@{
    ViewData["Title"] = "Index";
}
<div class="container p3">
    <div class="row pt-4">
        <div class="col-6">
            <h1>Index</h1>
        </div>
        <div class="col-6 text-end">
            <a class="btn btn-primary" asp-action="Create"><i class="bi bi-plus-circle"></i>&nbsp;Create New</a>
        </div>
    </div>
    <br /><br />

    <table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a class="btn btn-success" asp-action="Edit" asp-route-id="@item.id"><i class="bi bi-pencil-square"></i> &nbsp;Edit</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.id"><i class="bi bi-x-circle-fill"></i> &nbsp;Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

</div>




